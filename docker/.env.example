# Network Optimizer Environment Configuration
# Copy this file to .env and update with your values

# ===== Port Configuration =====
# Blazor Web UI port (includes metrics API)
WEB_PORT=8042

# InfluxDB port
INFLUXDB_PORT=8086

# Grafana port
GRAFANA_PORT=3000

# ===== InfluxDB Configuration =====
# Initial admin username
INFLUXDB_USERNAME=admin

# Initial admin password (CHANGE THIS!)
INFLUXDB_PASSWORD=changeme_influxdb_password

# Admin API token (generate a secure random string)
# You can generate one with: openssl rand -base64 32
INFLUXDB_TOKEN=changeme_influxdb_token

# Organization name
INFLUXDB_ORG=network-optimizer

# Default bucket name
INFLUXDB_BUCKET=network_optimizer

# Data retention period (e.g., 30d, 90d, 1y, 0 for infinite)
INFLUXDB_RETENTION=30d

# ===== Grafana Configuration =====
# Grafana admin username
GRAFANA_USERNAME=admin

# Grafana admin password (CHANGE THIS!)
GRAFANA_PASSWORD=changeme_grafana_password

# ===== Timezone Configuration =====
# Set your timezone (e.g., America/Chicago, America/New_York, Europe/London)
TZ=UTC

# ===== Network Optimizer Application Settings =====
# UniFi Controller URL (optional, can be configured via web UI)
# UNIFI_CONTROLLER_URL=https://192.168.1.1

# UniFi Controller credentials (optional, can be configured via web UI)
# UNIFI_USERNAME=
# UNIFI_PASSWORD=

# License key (optional, can be configured via web UI)
# LICENSE_KEY=

# ===== Advanced Settings =====
# Enable debug logging
# DEBUG=false

# Agent authentication token (auto-generated if not set)
# AGENT_AUTH_TOKEN=

# ===== Speed Test Server Identity (Fallback) =====
# The app auto-detects the source IP from iperf3 test results, which is the most
# accurate method. This setting is only needed as a fallback when:
# - Running path analysis without an iperf3 test (e.g., on page load)
# - The iperf3 output parsing fails for some reason
# In most cases, you don't need to set this.
# HOST_IP=192.168.1.220
